(ns funnyball.build-test
  (:require [clojure.test :refer :all]
            [funnyball.build :refer :all]))

(deftest a-test
  (testing "FIXME, I fail."
    (is (= 0 1))))

(deftest top-seeds-dataset-test
	testing "Finding the top seeds"
	()
)